////////////////////////////////////
// Statements and Expressions

3 + 4; //this is expression because it make value
2000; //this is expression because it make value
true && false && !false; //this is expression because it make value

if (23 > 10) {
  //this is statement
  const str = "23 is bigger";
}

const me = "Amir";
console.log(`I'm ${2037 - 2000} years old ${me}`);
